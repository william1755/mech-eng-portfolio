{
  "name": "mech-eng-portfolio",
  "version": "1.0.0",
  "description": "Portfolio site for a mechanical engineer",
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "npm run build && touch docs/.nojekyll && git add docs/ && git commit -m 'Deploy site' && git push"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}