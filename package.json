{
  "name": "mech-eng-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "eleventy",
    "predeploy": "rm -rf docs && npm run build && touch docs/.nojekyll",
    "deploy": "npm run predeploy && git add docs/ && git commit -m 'Deploy to GitHub Pages' && git push",
    "start": "eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "gh-pages": "^6.3.0"
  }
}
